name: Deferred - Bootstrap Echelon Engineering Capabilities

# REQ-BOOTSTRAP-001 tracks replacement of this bootstrap mechanism.
# GitHub Actions cannot safely self-bootstrap the generated workflow files
# with the repository GITHUB_TOKEN. Keep this file as evidence/documentation
# until the supported bootstrap mechanism is implemented.

on:
  workflow_dispatch:

jobs:
  deferred:
    runs-on: ubuntu-latest
    steps:
      - name: Explain deferred bootstrap
        run: |
          echo "Capability self-bootstrap is deferred under REQ-BOOTSTRAP-001."
          echo "Use an authorized development environment/repository tool for capability installation."
